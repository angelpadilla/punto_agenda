<div class="calendar-title has-text-neon bold">
  <%= t('date.month_names')[start_date.month].titleize %> <%= start_date.year %>
</div>
<%= render 'simple_calendar/actions', calendar: calendar %>
<div class="simple-calendar mt-4">
  <table>
    <thead>
      <tr>
        <% date_range.slice(0, 7).each do |day| %>
          <th><%= t('date.abbr_day_names')[day.wday].titleize %> </th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <% date_range.each_slice(7) do |week| %>
        <%= content_tag :tr, class: calendar.tr_classes_for(week) do %>
          <% week.each do |day| %>
            <%= content_tag :td, class: "#{calendar.td_classes_for(day).join(' ')} #{'not-working-day' unless @corp.working_day?(day)}" do %>
              <% instance_exec(day, calendar.sorted_events_for(day), &passed_block) %>
            <% end %>
          <% end %>
        <% end %>
      <% end %>
    </tbody>
  </table>
</div>
